### API is accessible
GET http://localhost:8080/api/health

> {%
    client.test("API is accessible", function () {
        client.assert(response.status == 200, "Wrong response status");
        client.assert(response.body.status == "UP", "Missing JWT Token");
    });
%}